plugins { id 'com.android.application' }

def geminiKey = System.getenv("GEMINI_API_KEY") ?: ""
def escapedGeminiKey = geminiKey.replace("\\", "\\\\").replace("\"", "\\\"")

android {
    namespace 'com.mahdihi69.pouya'
    compileSdk 35

    defaultConfig {
        applicationId 'com.mahdihi69.pouya'
        minSdk 23
        targetSdk 35
        versionCode 2
        versionName '1.1'
    }

    buildFeatures {
        buildConfig true
    }

    defaultConfig.buildConfigField 'String', 'GEMINI_API_KEY', '"' + escapedGeminiKey + '"'
}
